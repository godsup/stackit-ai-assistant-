apiVersion: v1
kind: Service
metadata:
  name: stackit-ai-frontend
  labels:
    app: stackit-ai-frontend
spec:
  selector:
    app: stackit-ai-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: LoadBalancer
